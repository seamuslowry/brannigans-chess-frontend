import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#9c003b'
    },
    secondary: {
      main: '#dcb153'
    }
  }
});

export default theme;
